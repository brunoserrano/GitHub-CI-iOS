plugin 'cocoapods-keys', {
  :project => "GitHub-CI",
  :keys => [
    "DANGER_GITHUB_API_TOKEN"
  ]
}

platform :ios, '12.0'
workspace 'GitHub-CI.xcworkspace'

target 'GitHub-CI' do
  use_frameworks!
end
